# Lab 13 Part 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update `lab13/rabbit.js` so it satisfies JSLint with only the minimal assignment-scoped fixes.

**Architecture:** Keep the existing single-file JavaScript structure. Resolve the reported JSLint issues by declaring globals properly, using the correct constructor call form, and allowing `console` through a JSLint directive without changing the assignment's intended behavior.

**Tech Stack:** JavaScript, JSLint, Node.js

---

### Task 1: Fix `rabbit.js`

**Files:**
- Modify: `lab13/rabbit.js`
- Test: manual JSLint check for `lab13/rabbit.js`

- [ ] **Step 1: Write the failing test**

There is no automated test harness in this lab folder, so use the existing JSLint output as the failing behavior record:

```text
1. Undeclared 'name'. Line 1:1
2. Unexpected 'this'. Line 3:3
3. Move variable declaration to top of function or script. Line 5:1
4. Expected 'new' before 'Rabbit'. Line 5:9
5. Undeclared 'console'. Line 7:1
6. Undeclared 'console'. Line 8:1
7. Undeclared 'name'. Line 8:13
```

- [ ] **Step 2: Verify the failure state**

Run the file through JSLint in the browser or course-approved setup and confirm the same warnings appear for `lab13/rabbit.js`.

Expected: JSLint reports the seven warnings listed above.

- [ ] **Step 3: Write the minimal implementation**

Update `lab13/rabbit.js` to:

```javascript
/*jslint devel */
var name = "Monty";
var r;

function Rabbit(name) {
    this.name = name;
}

r = new Rabbit("Python");

console.log(r.name);
console.log(name);
```

- [ ] **Step 4: Verify the fix**

Run JSLint again on `lab13/rabbit.js`.

Expected: the reported warnings are gone.

Run:

```bash
node "semester-2/aplp/lab13/rabbit.js"
```

Expected output:

```text
Python
Monty
```

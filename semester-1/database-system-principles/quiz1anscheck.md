### Quiz 1 Answer Check CS257

1. We use 1 byte at the end of a null terminated string. We need to store the length of the string. The number of bytes used to store the length is 2.
   a. Concept to review: Sizes of data types, how data types work in DB/DB2.
2. DISTINCT WITH COUNT (used together) does not count NULL
   a. Concept to review: SQL Clauses, and NULL’s relationship with other elements in SQL.
3. Consistency is guaranteed, not integrity, during a transaction.  
   a. Concept to review: ACID properties, Consistency v/s Integrity
4. Data could be flushed to the disk before the actual commit.  
   a. Concept to review: Atomicity, and violation of atomicity.
